module.exports = {

    // Mercado padrão
    DEFAULT_SYMBOL: "stpRNG5",

    // Timeframe do Candle (em segundos)
    TIMEFRAME_SECONDS: 5,

    // Ambiente
    MODE: "demo",

    // Inicialização
    AUTO_CONNECT: true,
    AUTO_SUBSCRIBE: true,

    // Buffer de ticks
    MAX_TICKS: 100,

    // Histórico de candles
    MAX_CANDLES: 500,

    // Execução
    DEFAULT_STAKE: 1,
    DEFAULT_DURATION: 5

};